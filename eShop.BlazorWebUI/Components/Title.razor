@inject IJSRuntime JS

@code {
    [Parameter] public string Text { get; set; } = string.Empty;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        await JS.InvokeVoidAsync("setTitle", Text);
    }
}
